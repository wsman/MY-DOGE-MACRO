# 宪法驱动开发核心索引 (CDD Core Index)

**版本**: v2.0.0-alpha
**状态**: 🟢 生产就绪 (Production Ready)
**说明**: 本文件是MY-DOGE-MACRO项目的宪法入口索引，依据§152单一真理源公理，提供项目核心架构的快速导航。

---

## 1. 项目核心理念

MY-DOGE-MACRO是一个基于宪法驱动开发(CDD)的量化交易分析系统，集成三大数据源：
- **DeepSeek AI**: 宏观分析与策略生成
- **Yahoo Finance**: 全球资产价格数据
- **通达信数据库**: 本地A股/美股历史数据

**核心原则**: 熵减优化、单一真理源、架构同构性。

**部署方式**: Docker容器化部署，支持开发和生产环境。

---

## 2. T0-T3分层架构

依据基本法§104功能分层拓扑公理，系统严格遵循T0-T3分层架构：

| 层级 | 名称 | 用途 | 位置 |
|------|------|------|------|
| **T0** | 核心意识层 | 常驻内存，系统自我意识 | `memory_bank/t0_core/` |
| **T1** | 索引与状态层 | 高频检索，系统状态跟踪 | `memory_bank/t1_axioms/` |
| **T2** | 执行规范层 | 按需加载，开发标准与工作流 | `memory_bank/t2_standards/`, `memory_bank/t2_protocols/` |
| **T3** | 分析与归档层 | 离线存储，文档与归档 | `memory_bank/t3_documentation/` (扁平结构) |

**数学约束**:
- $\sum_{file \in Context} \text{Token}(file) \leq T_{limit}$ (约128K tokens)
- $\text{Attention}(T0) \gg \text{Attention}(T1) > \text{Attention}(T2)$

---

## 3. CDD流程概览

依据程序法§201，宪法驱动开发遵循五状态工作流：

```mermaid
graph LR
    A[State A: 基准摄入] --> B[State B: 文档规划]
    B -->|用户批准| C[State C: 受控执行]
    C --> D[State D: 三级验证]
    D -->|验证通过| E[State E: 收敛纠错]
    D -->|验证失败| C
```

**核心原则**: 文档先行，代码随后。严禁直接修改代码，必须先修改文档并获批准。

---

## 4. 核心宪法索引

依据§152单一真理源公理，所有宪法文件现在集中存储在`memory_bank/t0_core/`目录：

### 4.1 宪法内核 (T0 Core)
| 文件 | 用途 | 路径 |
|------|------|------|
| **基本法索引** | 核心公理与架构约束 | `memory_bank/t0_core/basic_law_index.md` |
| **程序法索引** | 工作流与操作流程 | `memory_bank/t0_core/procedural_law_index.md` |
| **技术法索引** | 开发标准与规范 | `memory_bank/t0_core/technical_law_index.md` |
| **知识图谱** | 系统实体关联导航 | `memory_bank/t0_core/knowledge_graph.md` |
| **活跃上下文** | 当前系统状态与任务 | `memory_bank/t0_core/active_context.md` |

### 4.2 索引导航
- **快速启动**: 加载`active_context.md` → 查阅相关索引 → 执行对应工作流
- **开发需求**: 查阅`technical_law_index.md` → 找到对应标准 → 按规范实施
- **流程执行**: 查阅`procedural_law_index.md` → 找到对应工作流 → 按步骤执行
- **架构问题**: 查阅`knowledge_graph.md` → 图谱导航 → 找到解决方案

---

## 5. 宪法依据

### 5.1 核心公理引用
- **§152单一真理源公理**: `memory_bank`是可执行规范的唯一真理源，引用而非复制
- **§102.3宪法同步公理**: 版本变更必须触发全体系同步扫描与强制对齐
- **§141熵减验证公理**: 重构必须满足语义保持性($S' = S$)和熵减验证($H' \leq H$)
- **§104功能分层拓扑公理**: 系统严格遵循T0-T3分层架构，层级间单向依赖

### 5.2 精简操作记录
- **操作时间**: 2026-02-14
- **操作内容**: 版本升级至v2.0.0-alpha，同步Docker容器化架构
- **宪法依据**: §102.3宪法同步公理，版本同步与架构对齐
- **当前状态**: 真理源统一至`memory_bank/t0_core/`，本文件`.clinerules`作为项目根目录下的宪法入口索引

---

## 6. 使用指南

### 6.1 新开发者入门
1. 阅读本文件了解项目核心理念
2. 查看`memory_bank/t0_core/active_context.md`了解当前状态
3. 根据需要查阅对应的宪法索引文件
4. 遵循CDD流程(§201)进行开发

### 6.2 问题解决流程
1. 使用`knowledge_graph.md`进行图谱导航
2. 查找相关宪法条款(基本法、程序法、技术法)
3. 执行对应的工作流或标准
4. 验证解决方案的宪法合规性

### 6.3 架构变更流程
1. 更新`memory_bank/t0_core/`中的宪法文件
2. 执行三级验证协议(§156)
3. 更新本索引文件
4. 记录变更到`active_context.md`

### 6.4 Docker部署
```bash
# 开发模式 (端口3000，热重载)
docker-compose -f docker-compose.dev.yml up --build

# 生产模式 (端口80)
docker-compose up -d --build
```

---

## 7. 版本信息

| 组件 | 版本 | 状态 |
|------|------|------|
| **项目版本** | MY-DOGE-MACRO v2.0.0-alpha | 🟢 生产就绪 |
| **宪法版本** | v2.0.0-alpha | 🟢 本索引 |
| **CDD框架** | v1.6.1 | 🟢 运行中 |
| **架构版本** | 模块化 v2.0.0 | ✅ Docker容器化 |

**最后更新**: 2026-02-14  
**宪法驱动开发**: 代码即数学证明，架构即宪法约束。

---

> **重要提示**: 所有详细宪法内容请查阅`memory_bank/t0_core/`目录下的具体文件。本文件仅为入口索引，具体实施必须引用真理源。

---

## 8. 北欧主题设计规范 (Nordic Theme)

### 8.1 设计理念
- **Hygge**: 舒适温馨的用户体验
- **Lagom**: 恰到好处的视觉设计
- **负空间美学**: 克制的装饰，强调内容

### 8.2 禁止硬编码颜色类 (严格规则)

#### ❌ 禁止使用的 Tailwind 颜色类
严禁使用以下硬编码颜色模式：
- `bg-{color}-{shade}` - 如 `bg-blue-600`, `bg-red-500`
- `text-{color}-{shade}` - 如 `text-gray-400`, `text-green-600`
- `border-{color}-{shade}` - 如 `border-red-500`, `border-slate-300`
- `ring-{color}-{shade}` - 如 `ring-blue-500`

**禁止颜色列表**: red, blue, green, yellow, gray, slate, emerald, amber, cyan, purple, pink, orange, indigo, violet, fuchsia, rose, sky, teal, lime

#### ✅ 必须使用的北欧语义化颜色

**背景色 (Background)**:
| 语义类 | 用途 | 对应CSS变量 |
|--------|------|-------------|
| `bg-[var(--bg-primary)]` | 主背景 | `--nordic-white` / `--nordic-night` |
| `bg-[var(--bg-secondary)]` | 次背景 | `--nordic-snow` / `--nordic-charcoal` |
| `bg-[var(--bg-tertiary)]` | 三级背景 | `--nordic-frost` / `--nordic-slate` |
| `bg-[var(--bg-elevated)]` | 浮起元素背景 | `#FFFFFF` / `#242830` |
| `bg-[var(--accent-bg)]` | 强调色背景 | `--nordic-pine-light` |

**文字色 (Text)**:
| 语义类 | 用途 | 对应CSS变量 |
|--------|------|-------------|
| `text-[var(--text-primary)]` | 主文字 | `--nordic-charcoal` / `--nordic-snow` |
| `text-[var(--text-secondary)]` | 次文字 | `--nordic-granite` / `--nordic-cloud` |
| `text-[var(--text-tertiary)]` | 辅助文字 | `--nordic-stone` |
| `text-[var(--text-inverse)]` | 反色文字 | `--nordic-white` / `--nordic-night` |

**强调色 (Accent)**:
| 语义类 | 用途 | 对应CSS变量 |
|--------|------|-------------|
| `bg-[var(--accent-primary)]` | 主强调背景 | `--nordic-pine` (#3D7A5F) |
| `text-[var(--accent-primary)]` | 主强调文字 | `--nordic-pine` |
| `bg-[var(--accent-hover)]` | 强调悬停 | `--nordic-pine-hover` |

**状态色 (Status)**:
| 语义类 | 用途 | 对应CSS变量 |
|--------|------|-------------|
| `text-[var(--status-success)]` | 成功状态 | `--nordic-aurora-green` (#7FBC8C) |
| `text-[var(--status-warning)]` | 警告状态 | `--nordic-aurora-amber` (#E4A853) |
| `text-[var(--status-error)]` | 错误状态 | `--nordic-aurora-rose` (#D16B6B) |
| `text-[var(--status-info)]` | 信息状态 | `--nordic-fjord` (#4A6FA5) |

**边框色 (Border)**:
| 语义类 | 用途 | 对应CSS变量 |
|--------|------|-------------|
| `border-[var(--border-primary)]` | 主边框 | `--nordic-mist` / `--nordic-slate` |
| `border-[var(--border-secondary)]` | 次边框 | `--nordic-cloud` / `--nordic-granite` |

### 8.3 宪法依据
- **§102 熵减原则**: 简洁即美，功能即形式
- **§152 单一真理源**: 所有颜色定义来自 `libs/design-system/themes/nordic-minimal.css`
- **§103 一致性公理**: 统一使用语义化颜色确保主题一致性

### 8.4 使用示例

```tsx
// ❌ 错误 - 硬编码颜色
<button className="bg-blue-600 text-white hover:bg-blue-700">
  提交
</button>

// ✅ 正确 - 语义化颜色
<button className="nordic-btn nordic-btn--primary">
  提交
</button>

// ✅ 正确 - 使用CSS变量
<div className="bg-[var(--bg-primary)] text-[var(--text-primary)] border-[var(--border-primary)]">
  内容
</div>
```

### 8.5 主题文件位置
```
libs/design-system/themes/
├── nordic-minimal.css    # 核心CSS变量和组件样式
├── NordicThemeUtils.ts   # 工具函数
├── NordicThemeContext.tsx # React Context
└── index.ts              # 统一导出